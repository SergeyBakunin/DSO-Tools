# Переменные окружения для развертывания на NAS
# Скопируйте этот файл в .env и настройте под ваш NAS

# Доменное имя или IP адрес вашего NAS
NAS_DOMAIN=dso.deone37.synology.me
NAS_IP=192.168.0.233

# Timezone (найдите свой на https://en.wikipedia.org/wiki/List_of_tz_database_time_zones)
TZ=Europe/Moscow

# Порты (ВАЖНО: должны совпадать с настройками Reverse Proxy)
# Backend будет доступен через обратный прокси на порту 8090
BACKEND_PORT=8090
# Frontend можно оставить на 3000 или настроить отдельный reverse proxy
FRONTEND_PORT=3000

# Пути к данным на NAS (для Synology обычно /volume1, для QNAP - /share)
NAS_VOLUME=/volume1
DATA_PATH=${NAS_VOLUME}/docker/devsecops

# Log level (debug, info, warning, error)
LOG_LEVEL=info

# Resource limits для Synology DS723+
# DS723+ имеет AMD Ryzen R1600 (2 cores, 4 threads) и минимум 2GB RAM
BACKEND_CPU_LIMIT=1.0
BACKEND_MEMORY_LIMIT=512M
FRONTEND_CPU_LIMIT=0.5
FRONTEND_MEMORY_LIMIT=256M

# URL для API (используется frontend для обращения к backend)
# Через reverse proxy: https://dso.deone37.synology.me
# Локально: http://localhost:8090
API_URL=https://dso.deone37.synology.me
