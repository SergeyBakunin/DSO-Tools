# Docker - Быстрый старт

## 🚀 Запуск за 1 минуту

### Вариант 1: Через командную строку

```bash
# Перейдите в папку проекта
cd "C:\Users\Sergey Bakunin\sbom-tools"

# Запустите всё одной командой
docker-compose up -d
```

**Готово!** Приложение запущено:
- 🌐 Frontend: http://localhost:3000
- 🔧 Backend API: http://localhost:8000
- 📚 API Docs: http://localhost:8000/docs

---

### Вариант 2: Через VS Code (рекомендуется)

1. **Откройте проект** в VS Code
2. **Найдите файл** `docker-compose.yml` в проводнике
3. **Щёлкните правой кнопкой** на файле
4. Выберите **"Compose Up"**
5. **Готово!** VS Code автоматически соберёт и запустит контейнеры

---

## 📊 Проверка статуса

### Через командную строку:
```bash
docker-compose ps
```

### Через VS Code:
1. Откройте **Docker панель** (иконка Docker слева)
2. Раздел **"Containers"**
3. Должны быть запущены:
   - `devsecops-backend` ✅
   - `devsecops-frontend` ✅

---

## 📝 Просмотр логов

### Через командную строку:
```bash
# Все логи
docker-compose logs -f

# Только backend
docker-compose logs -f backend

# Только frontend
docker-compose logs -f frontend
```

### Через VS Code:
1. **Docker панель** → Containers
2. **Щёлкните правой кнопкой** на контейнере
3. Выберите **"View Logs"**

---

## 🛑 Остановка

### Через командную строку:
```bash
docker-compose down
```

### Через VS Code:
1. Найдите файл `docker-compose.yml`
2. **Щёлкните правой кнопкой**
3. Выберите **"Compose Down"**

---

## 🔄 Перезапуск

### Через командную строку:
```bash
docker-compose restart
```

### Через VS Code:
1. **Docker панель** → Containers
2. **Щёлкните правой кнопкой** на контейнере
3. Выберите **"Restart"**

---

## 🔍 Что происходит при запуске?

1. **Сборка образов** (первый раз):
   - Backend: Python 3.13 + FastAPI
   - Frontend: React → Nginx

2. **Создание сети**:
   - `devsecops-network` для связи между контейнерами

3. **Запуск контейнеров**:
   - Backend на порту 8000
   - Frontend на порту 3000

4. **Health checks**:
   - Автоматическая проверка здоровья контейнеров

---

## ⚠️ Возможные проблемы

### Порт уже занят

**Ошибка:** `port is already allocated`

**Решение:**
```bash
# Остановите процесс на порту 8000 или 3000
# Или измените порты в docker-compose.yml
```

### Docker не запущен

**Ошибка:** `Cannot connect to the Docker daemon`

**Решение:**
1. Запустите **Docker Desktop**
2. Подождите, пока Docker полностью запустится
3. Попробуйте снова

### Недостаточно памяти

**Решение:**
1. Docker Desktop → Settings → Resources
2. Увеличьте Memory до минимум 4GB
3. Нажмите "Apply & Restart"

---

## 📖 Подробная документация

Для детальной информации см. [DOCKER_GUIDE.md](DOCKER_GUIDE.md)

---

**Всё готово! Приложение запущено и доступно по адресу http://localhost:3000** 🎉
